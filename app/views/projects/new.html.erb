<div class = "span4 corner_gradient">
<h1>New <%= @project.parent ? t("sub_project") : t("project") %></h1>

<%= render 'form' %>

<%#= link_to t('subscription.show', :default => 'Show/upgrade subscription'), subscription_show_path, :class => 'btn' %>
</div>